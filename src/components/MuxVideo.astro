---
import MuxVideoReact from './MuxVideoReact.tsx';

interface Props {
  playbackId: string;
  title?: string;
  aspectRatio?: string;
}

const { playbackId, title, aspectRatio } = Astro.props;
---

<MuxVideoReact client:load playbackId={playbackId} title={title} aspectRatio={aspectRatio} />
