---
import MuxVideoReact from './MuxVideoReact.tsx';

interface Props {
  playbackId: string;
  title?: string;
}

const { playbackId, title } = Astro.props;
---

<MuxVideoReact client:load playbackId={playbackId} title={title} />
